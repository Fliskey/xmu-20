DATA SEGMENT
X DB 4
Y DB 5
Z DB ?
Z1 DB ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
	MOV AX,DATA
	MOV DS,AX
	MOV AL,X
	ADD AL,Y
	MOV BL,8
	IMUL BL
	MOV BL,X
	MOV BH,0
	SUB AX,BX
	MOV BL,2
	IDIV BL
	MOV Z,AL
	MOV Z1,AH
	MOV AH,4CH
	INT 21H
	CODE ENDS
END START
