#include <stdio.h>
#include <string.h>
#define P 32768

int await[32800] = {0};

int main(){
    int t;
    scanf("%d",&t);
    while(t>=1){
    	
    	memset(await,0,sizeof(await));
        int n;
        scanf("%d",&n);
        int x[104];
        for(int i=1;i<=n;i++){
        	scanf("%d",&x[i]);
		}
		
		for(int i=1;i<=)
		
        t--;
    }
    return 0;
}
