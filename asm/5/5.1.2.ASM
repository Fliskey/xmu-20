DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV BX,0D60AH
    MOV DL,0
JUDGE:    CMP BX,0
    JE	DONE
    SHR BX,1
    ADC DL,0
    JMP JUDGE
DONE:    MOV AX,6666H
	
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
