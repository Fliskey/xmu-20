#include <stdio.h>
#include <string.h>

struct{
    int cost;
    int time;
}map[14][14][14];

int main(){
    int n,m;
    memset(map,-1,sizeof(map));

    scanf("%d %d",&n,&m);
    for(int i=1;i<=n;i++){
        
    }

    return 0;
}